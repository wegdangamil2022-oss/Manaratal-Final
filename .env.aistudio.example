# Google AI Studio: copy to .env.aistudio.local (gitignored), or use the environment UI.
# No secret is needed for Web preview. VITE_* values are exposed to the browser.
# Use only an independent experimental API/database. Never copy original service credentials.
MANARATAK_GOOGLE_AI_STUDIO=true
MANARATAK_RUNTIME_PROFILE=google-ai-studio
VITE_API_URL=
VITE_API_BASE_URL=
VITE_PUBLIC_WEB_URL=
VITE_ADMIN_URL=
VITE_PUBLIC_TEMPLATE_DATA_MODE=api
VITE_LOCAL_ADMIN_READ_ONLY=false
DISABLE_HMR=false

# Backend/tooling inventory: placeholders only, intentionally commented out.
# Do not uncomment all entries. Configure only a separately approved Full Runtime capability.
# Required status and definition links: docs/operations/GOOGLE_AI_STUDIO_ENVIRONMENT.md
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# ACCESS_TOKEN_TTL_SECONDS=
# Server configuration; Required for staging/production Full API; not Web preview
# ADMIN_AUTH_MODE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# ADMIN_BEARER_TOKEN=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ADMIN_BOOTSTRAP_EMAIL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ADMIN_BOOTSTRAP_ID=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# ADMIN_BOOTSTRAP_PASSWORD=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ADMIN_URL=
# Server configuration; Required for staging/production Full API; not Web preview
# ADMIN_WEB_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# ALLOW_DATABASE_MUTATIONS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ALLOW_DISPOSABLE_DB_MUTATIONS=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# ALLOW_PRODUCTION_DATABASE_MUTATIONS=
# Server configuration; Required for staging/production Full API; not Web preview
# API_BASE_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# APPLIED_MIGRATIONS=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_AI_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_CMS_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_FINANCE_RECONCILIATION_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_IMPORT_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_NOTIFICATION_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_RETENTION_CRON=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_WORKER_BATCH_SIZE=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_WORKER_ENABLED=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_WORKER_HEARTBEAT_MS=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_WORKER_INTERVAL_MS=
# Server configuration; Required for staging/production Full API; not Web preview
# BACKGROUND_WORKER_LEASE_MS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# BASE_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# BYTES=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# CERTIFICATE_COMPLETION_WORKER_=
# Server configuration; Required for staging/production Full API; not Web preview
# CERTIFICATE_COMPLETION_WORKER_ENABLED=
# Server configuration; Required for staging/production Full API; not Web preview
# CERTIFICATE_COMPLETION_WORKER_INTERVAL_MS=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# CERTIFICATE_PUBLIC_VERIFICATION_BASE_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# CERTIFICATE_SIGNING_KEY_REFERENCE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# CERTIFICATE_SIGNING_SECRET=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# CI=
# Server configuration; Required for staging/production Full API; not Web preview
# CORS_ORIGIN=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# COURSE_PERSISTENCE_TEST_DATABASE_IS_DISPOSABLE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# COURSE_PERSISTENCE_TEST_DATABASE_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# COURSE_PROVIDER_TEST_DATABASE_IS_DISPOSABLE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# COURSE_PROVIDER_TEST_DATABASE_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# COURSES=
# Server / restricted; Required for staging/production Full API; not Web preview
# CSRF_SECRET=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_BASELINE_OUTPUT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_DISPOSABLE_CONFIRM_TARGET=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_DISPOSABLE_REMOTE_APPROVED=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_ENVIRONMENT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_MUTATION_ENVIRONMENT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_MUTATION_PURPOSE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_MUTATION_TARGET=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_MUTATIONS_ALLOWED=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_PRODUCTION_CHANGE_ID=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_PROVISIONING_GATE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_RECOVERY_EVIDENCE_FILE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_RECOVERY_GATE_TOKEN=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_REQUIRED=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# DATABASE_TARGET_CLASS=
# Server / restricted; Required for staging/production Full API; not Web preview
# DATABASE_URL=
# Server configuration; Required for experimental direct connection; not Web preview
# DIRECT_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# DUMP_FILE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# EXPECTED_MIGRATIONS=
# Server configuration; Required for staging/production Full API; not Web preview
# FINANCE_BANK_PROVIDER_KEY=
# Server configuration; Required for staging/production Full API; not Web preview
# FINANCE_FX_PROVIDER_KEY=
# Server configuration; Required for staging/production Full API; not Web preview
# FINANCE_PAYMENT_PROVIDER_KEY=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# FINANCE_PROVIDER_ALLOW_INSECURE_HTTP=
# Server / restricted; Required for staging/production Full API; not Web preview
# FINANCE_PROVIDER_API_KEY=
# Server configuration; Required for staging/production Full API; not Web preview
# FINANCE_PROVIDER_BASE_URL=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# FINANCE_PROVIDER_MAX_ATTEMPTS=
# Server / restricted; Required for staging/production Full API; not Web preview
# FINANCE_PROVIDER_SIGNING_SECRET=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# FINANCE_PROVIDER_TIMEOUT_MS=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# GH_TOKEN=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# GITHUB_ACTOR=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# GITHUB_RUN_ATTEMPT=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# GITHUB_RUN_ID=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# GITHUB_SHA=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# INVALID_INDEXES=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ISO3=
# Server / restricted; Required for staging/production Full API; not Web preview
# JWT_ACTIVE_KEY_ID=
# Server / restricted; Required for staging/production Full API; not Web preview
# JWT_AUDIENCE=
# Server / restricted; Required for staging/production Full API; not Web preview
# JWT_ISSUER=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# JWT_PREVIOUS_PUBLIC_KEYS_JSON=
# Server / restricted; Required for staging/production Full API; not Web preview
# JWT_PRIVATE_KEY_PEM=
# Server / restricted; Required for staging/production Full API; not Web preview
# JWT_PUBLIC_KEY_PEM=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# JWT_SECRET=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# LOG_LEVEL=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_ASSET_PROVIDER_ALLOW_INSECURE_HTTP=
# Server / restricted; Required for staging/production Full API; not Web preview
# MANARATAK_ASSET_PROVIDER_API_KEY=
# Server configuration; Required for staging/production Full API; not Web preview
# MANARATAK_ASSET_PROVIDER_BASE_URL=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_ASSET_PROVIDER_MAX_RESPONSE_BYTES=
# Server / restricted; Required for staging/production Full API; not Web preview
# MANARATAK_ASSET_PROVIDER_SIGNING_SECRET=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_ASSET_PROVIDER_TIMEOUT_MS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_COMPOSE_ENVIRONMENT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_DATABASE_INTEGRATION_ENABLED=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_DEPLOYMENT_CONTROL_PLANE_TOKEN=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_DEPLOYMENT_CONTROL_PLANE_URL=
# Server configuration; Required for staging/production Full API; not Web preview
# MANARATAK_IMPORT_RAW_RETENTION_DAYS=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_LOCAL_ASSET_ROOT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_PREVIEW_DATABASE_PROBE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_SMOKE_API_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# MANARATAK_SMOKE_WEB_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# MIGRATIONS=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# NODE_ENV=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# NOTIFICATION_PROVIDER_ALLOW_INSECURE_HTTP=
# Server / restricted; Required for staging/production Full API; not Web preview
# NOTIFICATION_PROVIDER_API_KEY=
# Server configuration; Required for staging/production Full API; not Web preview
# NOTIFICATION_PROVIDER_BASE_URL=
# Server / restricted; Required for staging/production Full API; not Web preview
# NOTIFICATION_PROVIDER_SIGNING_SECRET=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# NOTIFICATION_PROVIDER_TIMEOUT_MS=
# Server configuration; Required for staging/production Full API; not Web preview
# NOTIFICATION_RECIPIENT_MAX_DELIVERIES_PER_HOUR=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# OTEL_EXPORT_INTERVAL_MS=
# Server configuration; Required for staging/production Full API; not Web preview
# OTEL_EXPORTER_OTLP_ENDPOINT=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# OTEL_SERVICE_NAME=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# OTEL_TRACES_SAMPLER_RATIO=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# OUT_DIR=
# Server configuration; Required for staging/production Full API; not Web preview
# OWNER_DOMAIN_OUTBOX_WORKER_ENABLED=
# Server configuration; Required for staging/production Full API; not Web preview
# OWNER_DOMAIN_OUTBOX_WORKER_INTERVAL_MS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PG_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PLAYWRIGHT_USE_INSTALLED_CHROME=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# PORT=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# POSTGRES_DB=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# POSTGRES_PASSWORD=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# POSTGRES_PORT=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# POSTGRES_USER=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PRISMA_BIN=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PRISMA_TELEMETRY_DISABLED=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PROVIDERS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# PUBLIC_PRERENDER_API_URL=
# Server configuration; Required for staging/production Full API; not Web preview
# PUBLIC_WEB_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_ASSET_MANIFEST=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_ASSET_READINESS_JSON=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_ASSET_RESTORE_DIR=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_DATABASE_BACKUP_FILE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_EVIDENCE_FILE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RECOVERY_POSTGRES_READINESS_JSON=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# REDIS_NAMESPACE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# REDIS_PASSWORD=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# REDIS_PORT=
# Server / restricted; Required for staging/production Full API; not Web preview
# REDIS_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RELEASE_PROMOTION_MANIFEST=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RESTORE_DB=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# RESTORE_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# ROOT=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# RUN_DATABASE_INTEGRATION_TESTS=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# RUN_DATABASE_TESTS=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# RUN_MAJOR_DATASET_INTEGRATION_TESTS=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# RUNTIME_CLOSURE_MODE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# SCHEMA=
# Server configuration; Required for staging/production Full API; not Web preview
# SECURE_COOKIE=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# SECURITY_CORS_ORIGINS=
# Server configuration; Required for staging/production Full API; not Web preview
# SECURITY_CSP_ENABLED=
# Server configuration; Required for staging/production Full API; not Web preview
# SECURITY_RATE_LIMIT_MAX=
# Server configuration; Required for staging/production Full API; not Web preview
# SECURITY_RATE_LIMIT_WINDOW_MS=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SESSION_SECRET=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# SESSION_TTL_SECONDS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# SHADOW_URL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# SOURCE_RUN_ID=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# SOURCE_SHA=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# SOURCE_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_ADMIN_PASSWORD=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_ADMIN_USER=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_DB_NAME=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_HOST=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_PASSWORD=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# SQL_USER=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# STORAGE_BASE_PATH=
# Server configuration; Required for staging/production Full API; not Web preview
# STUDENT_WORKSPACE_OUTBOX_WORKER_ENABLED=
# Server configuration; Required for staging/production Full API; not Web preview
# STUDENT_WORKSPACE_OUTBOX_WORKER_INTERVAL_MS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# TABLES=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# TARGET_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# TEST_FINANCE_SECRET=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# TEST_SESSION_ADMIN_DATABASE_URL=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# TEST_SESSION_APP_DATABASE_URL=
# Server configuration; Required for staging/production Full API; not Web preview
# TRUST_PROXY_HOPS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# UNEXPECTED_USER_SCHEMAS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# UNIVERSITY_IMPORT_ACTOR_ID=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# UNIVERSITY_IMPORT_APPROVAL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# UNVALIDATED_CONSTRAINTS=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# URL_OUTPUT=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# VERCEL=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# VERCEL_ENV=
# Browser / public (never secret); Optional/conditional; consult source contract for the enabled capability
# VITE_GOOGLE_AI_STUDIO=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# W3_DATABASE_TEST_DATABASE_IS_DISPOSABLE=
# Server / restricted; Optional/conditional; consult source contract for the enabled capability
# W3_DATABASE_TEST_DATABASE_URL=
# Server configuration; Optional/conditional; consult source contract for the enabled capability
# WP1_RECOVERY_GATE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_API_BASE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_APPROVALS_FILE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_ASSET_ID=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_AUTHORIZATION=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_CONFIRM_CONTROLLED_TRANSFER=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_COOKIE=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_ENABLE_DB_SENTINEL=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_OUTPUT_DIR=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_SOURCE_SYSTEM=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC08_WORKBOOK=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC10_AUTHORIZATION=
# Tooling / CI / optional capability; Optional/conditional; consult source contract for the enabled capability
# WPIC10_BASE_URL=
