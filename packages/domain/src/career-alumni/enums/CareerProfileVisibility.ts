export enum CareerProfileVisibility {
  PRIVATE = 'PRIVATE',
  PLATFORM = 'PLATFORM',
  PUBLIC = 'PUBLIC',
}

export enum AlumniProfileVisibility {
  PRIVATE = 'PRIVATE',
  ALUMNI_ONLY = 'ALUMNI_ONLY',
  PUBLIC = 'PUBLIC',
}
