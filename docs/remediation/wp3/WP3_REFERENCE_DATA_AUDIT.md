# WP3 Canonical Reference Data Audit

> **Operational supersession notice (2026-09-06):** Any instruction in this historical remediation artifact that requires an “Original Development Database”, a Google Studio recovery gate, or `WP1_RECOVERY_GATE` is superseded by [`docs/operations/GREENFIELD_DATABASE_PROVISIONING.md`](../../operations/GREENFIELD_DATABASE_PROVISIONING.md). This file remains historical/evidence context and is not current database-operation authority.

| Area | Before | After | Remaining limitation |
|---|---|---|---|
| Canonical identity | Country/Currency/Language DTOs exposed codes but hid persisted IDs | Repository responses expose existing stable IDs; resolver returns canonical ID plus standard code | Existing database values require Google Studio validation |
| Resolver boundary | No unified resolver for the five reference types | `IReferenceResolver` and `ReferenceResolverService` cover Country, Region, City, Language, Currency | Higher-domain adoption belongs to WP5/WP6 |
| Country/Region/City hierarchy | City could accept an incompatible region link | New writes verify active country and region-country compatibility | Existing rows and DB constraints require closure gate |
| Standard codes | Often consumed as raw strings | Contract classifies them as canonical external keys or aliases | Existing free-text consumers need domain-owned migration |
| Alias/historic/lifecycle | `isActive` plus metadata; no authoritative alias/successor model | Truth documented as PARTIAL/DEFERRED | Model and migration require DB approval |
| Study Destination ownership | Baseline implied a profile exposed by every country | Phase 07 owns identity only; downstream profile references `countryReferenceId` | Downstream persistence is deferred |
| Reference Admin | Canonical code CRUD; free-text region field | No marketing/workflow ownership found; capability classified honestly | Region, alias, and lifecycle management are partial/deferred |

## Consumer Findings

- Universities: `country` and `city` remain `LEGACY_STRING`; `UniversityIntegrationPayload` already anticipates canonical country/region/city IDs. Adoption and backfill are deferred to the owning package.
- International Tests: availability IDs are canonical-ready, but center `countryIso2Code`/`cityName` and fee `currencyCode` are unresolved external references. Resolver adoption is deferred to the owning package.
- Majors: no direct Country/Region/City/Language/Currency table access was found in the reviewed Phase 10 source.
- No higher domain directly queries Prisma Reference Data tables; persistence access remains inside `PrismaReferenceDataRepository`.

No Reference IDs were regenerated and no database operation was performed.
# 2026-08-13 Country Source Replacement

The six unreadable continent workbooks were replaced by the validated unified source:

`workspace/reference-data/countries/MANARATAK_All_Continents_Country_Records_CLEAN_IMPORT_READY.xlsx`

- SHA-256: `C896D03CC49C126EE907F342341737AE1E0E1AA023FB960D76DED17DC45804B9`
- Records: 194 countries across Africa, Asia, Europe, North America, South America, and Oceania.
- Required-field omissions: 0.
- Duplicate ISO2, ISO3, numeric ISO, slug, and public ID values: 0.
- Canonical source IDs use the supplied `ctry-<ISO2>` values; no identity was generated by remediation code.
- All records remain `UNREVIEWED`; source readiness does not constitute publication approval.
- `MANARATAK_Antarctica_Reference.xlsx` remains outside the active six-continent source and is not consumed by the dry-run pipeline.

Classification: `SOURCE_ARTIFACT_BLOCKER CLOSED FOR SIX-CONTINENT COUNTRY SOURCE`.

`npm run reference:countries:dry-run` validates the source without database access or writes. University Stage 1 dry-run resolves its country references through this source by ISO3 and supplied public ID. Database promotion remains blocked pending the approved runtime recovery gate.

## Country Import and Admin Preview Wiring

- `CountryImportPreviewService` maps the 29 source fields into the existing `UpsertReferenceCountryDto` contract and retains Arabic names, local name, numeric ISO, capital, currencies, languages, timezones, flag, slug, supplied public ID, review state, provenance, and notes in metadata.
- The mapped records pass through the existing `ReferenceDataImportHandoffService` and `ReferenceDataSeedPlanner`; no parallel persistence contract was introduced.
- `POST /api/v1/admin/reference-data/countries/import-preview` accepts normalized workbook rows and returns validation, duplicate, review, and promotion-blocker summaries.
- The Country admin tab can select an XLSX workbook and display its dry-run summary. It does not expose an Apply or Promote action.
- Promotion always reports `promotionAllowed: false` and `DATABASE_RECOVERY_GATE_REQUIRED`; all 194 supplied records additionally remain blocked by `SOURCE_REVIEW_REQUIRED` while their source status is `UNREVIEWED`.
- Database writes performed by preview: 0.

## Currency and Language Candidate Wiring

- The unified Country workbook contains usage codes, not complete authoritative ISO-4217 and ISO-639 catalogs.
- `CountryDerivedReferencePreviewService` extracts unique candidates, their roles, usage counts, and countries without converting them into canonical records.
- Current source result: 142 currency codes and 353 language codes.
- Suggested English display names use the local runtime only as review assistance; they are not source authority and are omitted when the runtime does not recognize a code.
- Suggested language direction is review assistance only and does not authorize persistence.
- `POST /api/v1/admin/reference-data/countries/derived-reference-preview` exposes the read-only candidate report.
- Currency and Language admin tabs can inspect candidates from the workbook. No bulk Apply or Promote command exists.
- Promotion blockers: `DATABASE_RECOVERY_GATE_REQUIRED`, `AUTHORITATIVE_CURRENCY_ENRICHMENT_REQUIRED`, and `AUTHORITATIVE_LANGUAGE_ENRICHMENT_REQUIRED`.
- Database writes performed: 0.

## Atomic Admin Mutation Adoption

- Active Admin upserts for Country, Currency, Language, and City now enter `PrismaAtomicPersistenceUnitOfWork`.
- The business record, required audit record, and transactional Outbox entry are written through the same transaction context.
- `PrismaReferenceDataRepository` exposes explicit transaction-only methods and rejects a missing Prisma transaction context.
- Audit or Outbox persistence failure propagates to the unit of work; the API cannot report business-only success.
- Outbox payloads contain identity and operation metadata only; source records and credentials are not copied into events.
- Legacy constructor use without the atomic executor remains available for isolated tests. Active Prisma API composition supplies the executor.
- Domain and Application TypeScript checks pass. Runtime Vitest execution is blocked because installed dependencies are isolated from the active module path; no install or dependency relocation was performed.
- Database rollback evidence remains pending the Google Studio recovery gate. Database writes performed during this adoption: 0.

The same atomic coordinator is now adopted by International Tests and Major Admin mutations; Reference Data remains the canonical reference owner and is not duplicated into either domain.

## Administrative Regions and Cities Source Dry Run

- Five Region CSV artifacts share one schema and contain 5,001 valid `(countryIso2, regionCode)` identities across all 194 canonical countries.
- Five City CSV artifacts share one schema and contain 8,991 valid unique city identities.
- Duplicate Region identities: 0. Duplicate City identities: 0. Missing technical Region references from City rows: 0.
- All 8,991 City rows remain `NEEDS_MANUAL_REVIEW` at source level.
- Region matching requires semantic review for 1,535 City rows: 55 fuzzy matches, 67 region-not-matched flags, 14 rows without an administrative name, and 1,399 unmatched-review flags.
- City files include 18 territory/special-identity codes outside the 194-country canonical source: `AW, CW, FO, GF, GG, GP, HK, IM, JE, KY, MO, MQ, PR, RE, TW, VG, XK, YT`.
- Six canonical countries currently have no City row: `GD, LI, NR, PS, PW, TV`.
- No territory was silently promoted to a Country and no City was discarded.
- `npm run reference:geography:dry-run` reproduces the audit without database access.
- Promotion remains blocked by database recovery, geography source review, country coverage reconciliation, and city-region semantic review.
- Database writes performed: 0.
