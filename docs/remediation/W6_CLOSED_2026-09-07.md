# MANARATAK — W6 Closure — 2026-09-07

**Wave:** W6 — Tests / Reliability / DevOps  
**Decision:** **W6 CLOSED — SOURCE VERIFIED**  
**External DB/provider/deployment/browser evidence:** **PENDING_NON_BLOCKING**

## Authority

- Remediation authority: `MANARATAK_REMEDIATION_EXECUTION_REGISTER_v0.68_ORDERED.md` / ordered W6 tasks 94–103.
- Input source baseline: `MANARATAK_FINAL_W5_SOURCE_CLOSED_2026-09-07.zip`.
- Input SHA256 verified before work: `a664a73b48d9399100956950305f3c55fbdaa90b7d8c757489f4ffa31ead60fe`.
- No older project ZIP was used and W0–W5 remediation was not reimplemented.
- W5 closure evidence was inherited as the dependency gate for this wave.

## W6 ordered-card disposition

| Order | Finding | W6 disposition |
| ---: | --- | --- |
| 94 | `MNT-AUD-0009` | `CLOSED_SOURCE` — translation quality verification no longer depends on brittle implementation literals; behavioral regression coverage preserves localization requirements through refactors. |
| 95 | `MNT-AUD-0010` | `CLOSED_SOURCE` — imported-course static security invariants now follow canonical force-reanalysis ownership in `CourseImportOperationsUseCases` and the live parser path; regression coverage protects the invariant. |
| 96 | `MNT-AUD-0047` | `CLOSED_SOURCE` — P16 verifier follows the live CMS public composition and contains no reference to deleted `CmsContentDetail.tsx`; current Phase 16 verifier passes 20/20. |
| 97 | `MNT-AUD-0048` | `CLOSED_SOURCE` — canonical source closure is manifest-driven, covers the active phase/domain verifier surface, and classifies external runtime gates explicitly as `PENDING_NON_BLOCKING`. |
| 98 | `MNT-AUD-0042` | `CLOSED_SOURCE` — disposable PostgreSQL whole-platform integration harness is authored and registered across P07–P21 persistence contracts; real DB execution is `PENDING_NON_BLOCKING`. |
| 99 | `MNT-AUD-0078` | `CLOSED_SOURCE` — production-capable OTLP/HTTP metrics and trace provider is wired through `MonitoringService`, HTTP middleware and worker observation; provider export/alert proof is `PENDING_NON_BLOCKING`. |
| 100 | `MNT-AUD-0066` | `CLOSED_SOURCE` — provider-neutral recovery contract, PITR/retention/immutable backup requirements, runtime readiness checks, whole-platform restore drill and scheduled audit workflow are source-implemented; live restore evidence is `PENDING_NON_BLOCKING`. |
| 101 | `MNT-AUD-0008` | `CLOSED_SOURCE` — architecture enforcement now governs active `scripts/**` operational tooling through an explicit classification boundary and regression tests. |
| 102 | `MNT-AUD-0087` | `CLOSED_SOURCE` — external GitHub Actions are pinned to immutable full 40-character SHAs, Dependabot update governance exists, and the SHA source gate passes 33 refs. |
| 103 | `MNT-AUD-0094` | `CLOSED_SOURCE` — deterministic immutable artifact/SBOM/checksum generation and exact-artifact validation→staging→production promotion are source-implemented with recovery/smoke gates; control-plane execution is `PENDING_NON_BLOCKING`. |

## Implemented W6 source surfaces

### Source-closure and CI authority

- machine-readable `scripts/ci/source-closure-manifest.json`;
- manifest verifier and runner;
- canonical CI integration plus source evidence recording;
- current phase/domain verifier registration;
- explicit runtime classification policy.

### Reliability and PostgreSQL validation

- disposable database safety harness;
- P07–P21 persistence integration suite registration;
- relationship/constraint/outbox/idempotency/advisory-lock coverage authored for a disposable PostgreSQL target.

### Observability

- `OtlpHttpMonitoringProvider` with OTLP/HTTP JSON metrics/traces envelopes;
- monitoring-service provider delegation for spans/flush/shutdown;
- HTTP request counters, duration, inflight and server spans;
- background-worker iteration telemetry;
- production telemetry configuration and readiness validation;
- alert-rule configuration and observability runbook.

### Disaster recovery

- production recovery contract with RPO/RTO/PITR/immutable-copy ownership;
- provider readiness verifier;
- whole-platform PostgreSQL + asset restore drill mechanics;
- scheduled recovery audit workflow;
- disaster-recovery runbook.

### Supply-chain and release governance

- immutable SHA pinning for external GitHub Actions;
- Dependabot governance for Action updates;
- deterministic release artifact build;
- SHA256 checksum and CycloneDX SBOM evidence;
- artifact verification;
- validation → staging → production promotion workflow;
- post-deployment smoke and rollback/control-plane hooks.

## Final W6 verification

Final source state passed:

- historical `verify-w6-source.mjs` — **5/5 PASS**; DB proof remains `PENDING_NON_BLOCKING`.
- `verify-w6-plan-closure.mjs` — **10/10 ordered W6 findings PASS**.
- W6 regression suite — **8/8 PASS**.
- Source Quality — **PASS**; package cycles 0, file cycles 0, accessibility findings 0.
- Source Architecture Guard — **PASS** including operational-tooling boundary.
- GitHub Action immutable pin gate — **PASS, 33 refs**.
- Phase 16 source verifier — **20/20 PASS**.
- Recovery source contract — **PASS**; runtime restore evidence explicitly pending.
- Source Closure Manifest — **PASS, 25 gates / 48 verifiers**.
- Imported-course static security audit — **PASS** with no failed invariant.
- changed JavaScript/MJS syntax — **21/21 PASS**.
- changed TypeScript/TSX syntax — **14/14 PASS** using the globally installed TypeScript parser.

Canonical W6 command authority is `npm run w6:verify`, which runs the historical W6 verifier and the ordered v0.68 W6 plan-closure verifier.

Evidence is stored under `docs/remediation/evidence/w6/`.

## Environment-deferred evidence

The following external checks are explicitly `PENDING_NON_BLOCKING` under the user's W6 instruction and `docs/remediation/W6_RUNTIME_PENDING_CHECKS.md`:

- disposable PostgreSQL integration execution;
- real OTLP telemetry export and alert delivery against the selected provider;
- production backup-readiness proof;
- whole-platform restore rehearsal against backup/object-storage artifacts;
- validation/staging/production deployment-control-plane promotion;
- post-deploy smoke and rollback rehearsal;
- live browser E2E.

A local `npm ci --ignore-scripts --no-audit --no-fund` attempt was made to expand environment-level verification, but the container transport timed out while npm remained active. Partial `node_modules` state was removed before packaging. This is recorded as `PENDING_ENVIRONMENT_DEPENDENCIES`; it is not represented as a PASS and is not included in the final ZIP.

No production database mutation, provider mutation, deployment, backup restore, or destructive rollback was executed during W6 source closure.

## Exit decision

**W6 is CLOSED at the source/remediation-wave boundary.**  
All ordered W6 source findings 94–103 have executable source evidence. External DB/provider/runtime/deployment checks remain explicitly `PENDING_NON_BLOCKING` and must not be converted into fabricated PASS results.

The next ordered remediation wave is **W7**.
